exports.formatCode = function(code) {

   
   //   const returned = code.split("\n").forEach(function(line, index) {
   //      console.log( line.replace("//", "#"));
   //   });
  
     return returned;
}